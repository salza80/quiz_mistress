.quiz-list
  .row
    - @quizzes.each do |quiz|
      .col-xs-12.col-sm-6.col-md-4
        .card.text-md-center.card-outline-primary
          .card-header.heading-bk
            %h4.card-title
              = quiz.title
          .card-block.text-md-center
            = image_full quiz.image
            = img_reference(quiz.image)
          .card-block
            .card-text
              = quiz.description
          .card-footer.text-md-center
            = link_to "Play Now", quiz, class: "btn btn-success"
