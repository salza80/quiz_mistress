%p#notice= notice
.quiz-list
  .row
    - @quizzes.each do |quiz|
      .col-xs-12.col-sm-6.col-md-4
        .card.text-md-center.card-outline-primary
          .card-header
            %h4.card-title
              = quiz.title
          .card-block
            = image_tag("/assets/images/" + quiz.main_image.path, class: "img-fluid")
          .card-block
            .card-text
              = quiz.description
          .card-footer.text-md-center
            = link_to "Play Now", quiz, class: "btn btn-success"
